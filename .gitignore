node_modules/
.env
.DS_Store
*.mp4
*.mov
*.jtl
k6-results*.json
k6-results*.csv
load-tests/reports/results/*.json
load-tests/reports/results/*.csv

# NOTE: requirments/ is intentionally TRACKED so the team's plan-study.md and the
# course briefs travel with the repo. Do not re-add `requirments/` here.
.claude/
CLAUDE.md